import React from 'react';

const Item = React.createClass({

  render: function () {
    // this.props.data.map(function (text) {
    return (
      <p>
        <span></span>
        <span></span>
      </p>
    );
    // })
  }
});
export default Item;
